/* INSERT INTO 테이블명
 * VALUES (모든 컬럼에 추가할 값);
 * 
 * INSERT INTO 테이블명 (컬럼1, 컬럼2, ...)
 * VALUES (컬럼1 값, 컬럼2 값, ...);
 * 
 * ------------------------------------------------
 * 
 * UPDATE 테이블명
 * SET
 * 			컬럼명 = 수정할 값;
 * 			컬럼명 = 수정할 값;
 * 			...
 * 
 * WHERE 
 * 			수정하려는 행을 찾는 조건
 * 
 * 
 * --------------------------------------------------
 * DELETE
 * FROM 테이블명 
 * WHERE 
 * 			삭제하려는 행을 찾는 조건;
 */
 
